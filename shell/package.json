{
  "name": "foodnest-pos-shell",
  "version": "0.1.0",
  "private": true,
  "description": "FoodNest POS desktop shell (Electron + Node orchestrating a PHP/SQLite backend)",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "electron": "^33.0.0"
  },
  "dependencies": {
    "node-thermal-printer": "^4.6.1"
  }
}
